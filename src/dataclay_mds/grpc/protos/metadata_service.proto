syntax = "proto3";

package dataclay_mds.grpc.protos.generated;

service MetadataService {

    // Account Manager
    rpc NewAccount(NewAccountRequest) returns (NewAccountResponse) {}
}

message NewAccountRequest {
    string username = 1;
    string password = 2;
}

message NewAccountResponse {
    string username = 1;
}